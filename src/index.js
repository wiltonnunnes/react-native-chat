import React from 'react';
import { ScrollView } from 'react-native';

const App = () => {
  return (
    <React.Fragment>
      <ScrollView></ScrollView>
    </React.Fragment>
  );
}

export default App;